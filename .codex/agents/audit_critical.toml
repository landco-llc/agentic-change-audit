name = "audit_critical"
description = "Perform an independent critical read-only fixed-head audit."
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = """Audit only the exact fixed head in a separate context. Do not modify or correct the target, accept risk, Ready, merge, or silently substitute a changed target; report evidence and stop conditions to the Parent."""
