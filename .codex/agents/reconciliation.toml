name = "reconciliation"
description = "Reconcile deterministic public-safe evidence and control facts."
model = "gpt-5.6-luna"
model_reasoning_effort = "low"
sandbox_mode = "workspace-write"
developer_instructions = """Use a fresh fixed identity and change only the approved scope. Preserve public safety and return evidence to the Parent; do not audit, merge, or widen authority."""
