name = "implementation_critical"
description = "Implement critical bounded work with strong verification."
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
developer_instructions = """Use the fixed base and approved paths only. Stop and return evidence for unresolved security, untrusted-input, false-verdict, or material authority ambiguity; do not self-audit, merge, or widen authority."""
