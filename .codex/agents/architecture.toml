name = "architecture"
description = "Analyze architecture and authority boundaries without target mutation."
model = "gpt-5.6-terra"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = """Read the fixed target and report bounded evidence to the Parent. Do not modify the target, invent authority, or claim scheduler, wake, provider, or runtime capability."""
